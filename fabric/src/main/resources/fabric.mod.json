{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",

  "name": "${mod_name}",
  "description": "${description}",
  "authors": [
    "${mod_author}"
  ],
  "contact": {
    "homepage": "${homepage}",
    "sources": "${sources}",
    "issues": "${issues}"
  },

  "license": "${license}",
  "icon": "${mod_id}.png",

  "environment": "*",
  "entrypoints": {
    "main": [
      "net.ps.weaponinfusion.fabric.FabricWeaponInfusion"
    ],
    "client": [
      "net.ps.weaponinfusion.fabric.client.FabricWeaponInfusionClient"
    ],
    "fabric-datagen": [
      "net.ps.weaponinfusion.fabric.datagen.ModDataGenerator"
    ]
  },
  "mixins": [
    "weaponinfusion.mixins.json",
    "weaponinfusion.fabric.mixins.json"
  ],
  "depends": {
    "java": ">=${java_version}",
    "minecraft": "${fabric_minecraft_version_range}",
    "fabricloader": ">=${fabric_loader_version}",
    "fabric-api": ">=${fabric_api_version}",
    "balm-fabric": ">=${balm_version}"
  },
  "suggests": {
  },
  "custom": {
    "modmenu": {
      "links": {
        "modmenu.discord": "${discord}"
      }
    }
  }
}
